@model IEnumerable<WareHouseManger.Models.EF.Employee>

@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <h1 class="page-header">Nhân viên</h1>


    <p>
        <a class="btn btn-sm btn-success" asp-action="Create"><i class="fa fa-plus-circle"> Thêm mới nhà cùng cấp</i></a>
    </p>

    <div class="panel panel-default">
        <div class="panel-body">
            <div class="table-responsive">


                <table class="table table-bordered table-sm table-hover table-striped">
                    <thead>
                        <tr>
                            <th>
                                Tên nhân viên
                            </th>
                            <th>
                                Số điện thoại
                            </th>
                            <th>
                                Địa chỉ
                            </th>
                            <th>
                                Email
                            </th>
                            <th class="text-center">
                                Tài khoản
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PhoneNumber)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Address)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EMail)
                                </td>
                                <td class="text-center">
                                    @if (item.Accounts.Count > 0)
                                    {
                                        <button class="btn btn-sm btn-success">Đang hoạt động</button>
                                    }
                                    else
                                    {
                                        <a asp-action="ActiveAcountConfirmed" asp-route-id="@item.EmployeeID"  class="btn btn-sm btn-danger">Kích hoạt</a>
                                    }
                                </td>
                                <td class="text-nowrap text-center" width="250px">
                                    <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@item.EmployeeID"> <i class="fa fa-edit"> Chỉnh sửa</i></a>
                                    <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@item.EmployeeID"><i class="fa fa-file"> Chi tiết</i></a>
                                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.EmployeeID"><i class="fa fa-trash-o"> Xóa</i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>