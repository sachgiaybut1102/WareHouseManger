@model WareHouseManger.Models.EF.Shop_Goods_Category

@{
    ViewData["Title"] = "Create";
}


<div class="card shadow container col-6">
    <div class="card-body">
        <h1>Thêm mới</h1>
        <h4>Loại hàng hóa</h4>
        <hr />
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label font-weight-bold">Tên loại hàng hóa</label>
                <input asp-for="Name" class="form-control" autofocus />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SortName" class="control-label font-weight-bold">Tên viết tắt</label>
                <input asp-for="SortName" class="form-control" autofocus />
                <span asp-validation-for="SortName" class="text-danger"></span>
            </div>
            <div class="panel-footer text-right">
                <input type="submit" value="Tạo" class="btn btn-primary" />
                <a class="btn btn-light" asp-action="Index">Quay lại</a>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
